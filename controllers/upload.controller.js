const db = require('../config/sql'); // connecting to the database

// show form
exports.upload = function (req, res, next) {
    res.render('upload', { // title and content variable that gets sent to our route
})};